import type { RoleLayoutBundle } from '../types';

// RoleLayout section labels (Context, Metrics, Stack, What I did, etc.).
export const roleLayout: RoleLayoutBundle = Object.freeze({
  interactive: 'Interactive · live demo',
  context: 'Context',
  metrics: 'Metrics',
  stack: 'Stack',
  what: 'What I did',
});

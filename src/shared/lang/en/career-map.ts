import type { CareerMapBundle } from '../types';

// Strings for the homepage CareerMap (and its child PinnedRoleCard).
export const careerMap: CareerMapBundle = Object.freeze({
  headline: 'Senior Full-Stack · Laravel + Vue · 10+ years · Tech Lead',
  previous: '// previous',
  currently: '// currently',
  deepDive: 'Deep-dive →',
});
